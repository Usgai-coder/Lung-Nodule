data:
  raw_lidc_dir: "./data/LIDC-IDRI"
  raw_luna_dir: "./data/LUNA16"
  processed_dir: "./data/processed"

preprocessing:
  min_diameter: 3            # 任务要求 6：提取直径≥3mm的结节
  min_agreement_readers: 3   # 任务要求 6：至少3位放射科医生一致认可
  label_mode: "binary"       # 构建二分类标签 (良性/恶性)